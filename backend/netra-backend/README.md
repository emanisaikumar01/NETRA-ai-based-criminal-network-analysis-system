# Netra Backend

Spring Boot backend for the Netra investigation platform.

## Run locally

```powershell
.\mvnw.cmd spring-boot:run
```

The API starts on `http://localhost:8080` by default. Use the `dev` or `prod` Spring profile for environment-specific settings.
